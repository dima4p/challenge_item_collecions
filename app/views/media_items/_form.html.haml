= simple_form_for(@media_item) do |f|
  = f.error_notification

  .form-inputs
    - if @media_item.new_record?
      = f.input :type
    = f.input :name
    = f.association :user
    = f.input :link
    = f.input :image

  .form-actions
    = f.button :submit
