== README

For the simplicity and speed up
1. sqlite3 instead of pg
2. turbolinks are not used
3. ActionMailer is not set up
4. guard is used
5. rubocop is not used
6. Only one locale is used and support is not included
7. Only one name is used for User (not 3)
8. Request specs are dropped
